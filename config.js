const config = {
  envFilename: '.env',
  paths: {
    gateway: '/Users/daniel.wood/Workspace/gateway-graphql-server/',
    falcon: '/Users/daniel.wood/Workspace/graphql-server/',
    // any other projects with an appropriately formatted .env file can go here
    // keys are used by the CLI to allow only changing specified projects if desired
  },
};
module.exports = config;
